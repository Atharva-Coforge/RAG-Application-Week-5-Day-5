# Add model-provider credentials when the provider is selected.
EMBEDDING_MODEL=
GENERATION_MODEL=

# Select one of: chroma, faiss, pgvector.
VECTOR_STORE=chroma
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/expense_rag
